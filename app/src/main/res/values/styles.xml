<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="cardViewStyle">@style/CardView</item>
        <item name="android:statusBarColor">@color/basicWhite</item>
        <item name="android:windowLightStatusBar">true</item>
        <!--Можно подумать надо стилем шрифтов-->
        <!--<item name="android:textViewStyle">@style/RobotoTextViewStyle</item>-->
    </style>

    <style name="TabTextBaseStyle" parent="@android:style/TextAppearance.Widget.TabWidget">
        <item name="android:fontFamily">sans-serif</item>
    </style>

    <style name="RobotoTextViewStyle" parent="android:Widget.TextView">
        <item name="android:fontFamily">@font/roboto_slab</item>
    </style>

    <style name="SettingActivityTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorLightGreen</item>
    </style>

    <style name="NotificationActivityTheme" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorAccent">@color/colorLightGreen</item>
    </style>

    <style name="SplashScreenTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/basicPurpleColorBackground</item>
        <item name="colorPrimaryDark">@color/basicPurpleColorBackground</item>
        <item name="colorAccent">@color/basicGrey</item>
        <item name="android:statusBarColor">@color/basicWhite</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="RatingBar" parent="@style/Base.Widget.AppCompat.RatingBar">
        <item name="android:progressTint">@color/ratingBarColorAccent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="Theme.AppCompat.Light.NoActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="Theme.AppCompat.Light"/>
</resources>